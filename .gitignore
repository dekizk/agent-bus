# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/
build/
dist/

# a venv accidentally created in the repo root (use .venv instead)
bin/
lib/
include/
share/
pyvenv.cfg

# macOS
.DS_Store

# Runtime state (rebuilt on startup)
events.db
events.db-wal
events.db-shm
.offsets/
pm_agent.lock
artifacts/
.agent-bus/
agent-bus.local.json
